// Pot.js
import React from 'react';

const Pot = ({ amount }) => {
    return (
        <div id="totalbet">
            <span>Pot Amount: {amount}</span>
        </div>
    );
};

export default Pot;
